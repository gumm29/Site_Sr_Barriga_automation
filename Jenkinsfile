pipeline{
    agent{ image 'ruby' }
    stages{
        stage('build'){
            steps{
                sh 'ruby --version'
                echo 'teste de build automatico'
            }
        }
    }
}
